# Secrets & credentials — NEVER commit
.env
agent_config.yaml

# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Generated audit artifacts (keep the dir, not the runtime output)
hireguard/workspace/notes/*.md
!hireguard/workspace/notes/.gitkeep

# Demo response cache (reproducible recording)
.cache/

# OS
.DS_Store

# Local editor / agent tooling (not project files)
.cursor/
